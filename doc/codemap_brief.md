# Code-map page — status

Built. The interactive hardware + memory atlas described here shipped as
`tools/codemap.html`, served at `/codemap/` alongside `/editor/` and
`/visualizer/` (see `doc/SITE-PLAN.md`, `.github/workflows/pages.yml`).

The original task brief — hardware pin table, memory/execution map, and the
design decisions it raised — is archived at
[`notesarchive/codemap-brief-archive.md`](../notesarchive/codemap-brief-archive.md).
